/** @type {import('tailwindcss').Config} */
const defaultTheme = require('tailwindcss/defaultTheme')

module.exports = {
  content: ["./src/js/*.js", "./src/*.html"],
  theme: {
    extend: {
    },

  },
  plugins: [],
}
